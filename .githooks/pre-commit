#!/usr/bin/env bash
set -euo pipefail

cd "$(git rev-parse --show-toplevel)"

if [[ ! -d vendor ]]; then
    echo "vendor/ not found; run 'composer install' before committing." >&2
    exit 1
fi

echo "Running composer check (cs:check + test)..."
composer check
